#
# ngt-zdotfiles: .zprofile
#

# if [ -z "$NGT_ZDOTFILES_LOADED" ]; 
# then
# 	#: load .zshrc
# 	if [ -f $HOME/.zshrc ]; then
# 		source $HOME/.zshrc
# 	elif [ -f $ZDOTDIR/.zshrc ]; then
# 		source $ZDOTDIR/.zshrc
# 	fi

# 	#: load .localrc
# 	if [ -f $HOME/.localrc ]; then
# 		source $HOME/.localrc
# 	fi
# fi